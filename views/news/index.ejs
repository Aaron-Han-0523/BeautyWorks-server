<!DOCTYPE html>
<html lang="ko">

<head>
  <%- include ("../partials/header.ejs") %>
</head>

<body class="layout-boxed">

  <%- include ("../partials/loader.ejs") %>

  <%- include ("../partials/client-nav.ejs") %>
  <%- include ("../partials/chatMark.ejs") %>

  <!--  BEGIN MAIN CONTAINER  -->
  <div class="main-container" id="container">

    <div class="overlay"></div>
    <div class="search-overlay"></div>

    <%- include ("../partials/client-sidebar.ejs") %>

    <!--  BEGIN CONTENT AREA  -->
    <div id="content" class="main-content">
      <div class="layout-px-spacing">

        <div class="middle-content container-xxl p-0">

          <!-- BREADCRUMB -->
          <div class="row px-3">
            <div class="breadcrumb align-items-end d-flex pt-3 br-20">
              <div class="col-3">
                <h6 class="main-menu">
                  <%= __('Lounge') %>
                </h6>
                <h4><strong>
                    <%= __('News') %>
                  </strong></h4>
              </div>
            </div>
          </div>

          <div class="row px-3 ">
            <div class="breadcrumb align-items-start d-flex br-20">
              <div class="col">
                <h5><strong>
                    <%= __('users.news.subtitle') %>
                  </strong></h5>
              </div>
            </div>
          </div>

          <!-- CONTENT AREA -->
          <div class="row px-3 justify-content-center">
            <div class="col-xxl-12 col-xl-12 col-md-12 col-sm-12 breadcrumb mt-1 py-0">

              <div class="widget w-100">


                <!-- News -->
                <div class="form-group ms-3">

                  <!-- list -->
                  <div class="row mt-3">
                    <div class="text-black align-self-center">
                      <div id="index_head" class="d-flex form-control text-center h6 p-0">
                        <div class="col-1 py-3">
                          <%= __('users.myPage.table.thead.number') %>
                        </div>
                        <div class="col-6 py-3">
                          <%= __('users.myPage.table.thead.title') %>
                        </div>
                        <div class="col-2 py-3">
                          <%= __('users.myPage.table.thead.date') %>
                        </div>
                        <div class="col-3 py-3">
                          <%= __('users.myPage.table.thead.writer') %>
                        </div>
                      </div>
                      <div id="index_body">
                        <% news.data.forEach((item,index)=>{ %>
                        <a class="d-flex border-bottom py-3 page-link text-black px-0 text-center" href='<%= codezip.url.users.news.main + `/` + item.id %>'>
                          <div class="col-1 text-ellipsis"><%= item.id %></div>
                          <div class="col-6 d-flex px-3">
                            <%- include('../partials/badge/new', {date:item.create_date}) %>
                            <div class="text-ellipsis">
                              <%= item.title %>
                            </div>
                          </div>
                          <div class="col-2 text-ellipsis">
                            <%= formatDate(item.create_date) %>
                          </div>
                          <div class="col-3 text-ellipsis">
                            <%# __('users.community.index.WrittenBy') %>
                            <%= __('admin.userName') %>
                          </div>
                        </a>
                        <% }) %>
                      </div>
                    </div>
                  </div>

                  <!-- pagination -->
                  <% if(+news.count) { %>
                  <!-- make_pagination_by_href = function (i18n_func, page, count, baseURL, limit = 10) -->
                  <%- make_pagination_by_href(
                    __,
                    +news.page,
                    +news.count,
                    codezip.url.users.news.main,
                    5
                  ) %>
                  <% } %>

                </div>
              </div>
            </div>

          </div>
        </div>

      </div>
    </div>
  </div>
  <!--  END CONTENT AREA  -->

  <%- include ("../partials/footer.ejs") %>

  </div>
  <!-- END MAIN CONTAINER -->

  <%- include ("../partials/commonScrpits.ejs") %>

  <!-- BEGIN PAGE LEVEL PLUGINS/CUSTOM SCRIPTS -->
  <script>


  </script>
  <!-- BEGIN PAGE LEVEL PLUGINS/CUSTOM SCRIPTS -->
</body>

</html>